jdk:
  - openjdk17
before_install:
  - echo "Before Install"
  - bash openjdk-17-jdk install
install:
  - echo "Install"
  - if ! bash openjdk-17-jdk use; then source ~/.sdkman/bin/sdkman-init.sh; fi
  - java -version
  - mvn install